# portage-overlay-clip

This overlay is for packages developped specifically for CLIP OS (be they for
CLIP-specific code, or for non-Gentoo packages - e.g. apt - that need to be
included in CLIP OS).
